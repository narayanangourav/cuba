{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cuba.serviceAccountName" . }}
  labels:
    {{- include "cuba.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
